module.exports = {
  plugins: {
    // Tailwind v4 uses a separate PostCSS adapter package
    // install with: npm install -D @tailwindcss/postcss
    '@tailwindcss/postcss': {},
    autoprefixer: {}
  }
}
